import { NumbertestPipe } from './numbertest.pipe';

describe('NumbertestPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbertestPipe();
    expect(pipe).toBeTruthy();
  });
});
