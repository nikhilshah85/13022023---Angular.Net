import { OfferpricePipe } from './offerprice.pipe';

describe('OfferpricePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferpricePipe();
    expect(pipe).toBeTruthy();
  });
});
